// We create this constants here and not in the cypress config env prop because in a real environment we will have multiple urls and useras
export const url = 'http://zero.webappsecurity.com/index.html'
export const login_username = 'username'
export const login_password = 'password'
